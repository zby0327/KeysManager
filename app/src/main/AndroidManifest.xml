<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.keysmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="stateAlwaysHidden|adjustPan">
        <activity android:name=".RestoreActivity"></activity>
        <activity android:name=".BackupActivity" />
        <activity
            android:name=".AboutActivity"
            android:label="更多信息" />
        <activity android:name=".EditKeyActivity" />
        <activity android:name=".InputPwdActivity" />
        <activity android:name=".AddKeyActivity" />
        <activity android:name=".ShowKeyActivity" />
        <activity
            android:name=".AddClusterActivity"
            android:theme="@style/TransDialog" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>